<% @gifs.each do |gif| %>
  <div class="stripe">
    <div class="container">
      <div class="row">
        <div class="col-md-4 col-md-offset-4">
          <a href="/gifs/<%= gif.id %>"><img src="<%= gif.url %>" alt=""></a>
        </div>
      </div>
    </div>
  </div>
<% end %>
<br>